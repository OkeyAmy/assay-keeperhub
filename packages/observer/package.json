{
  "name": "@assay/observer",
  "version": "0.1.0",
  "type": "module",
  "description": "Independent chain read layer. Must never import an execution provider — a verifier that reads through the executor agrees with it by construction.",
  "license": "Apache-2.0",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./rpc": "./src/rpc.ts",
    "./blockscout": "./src/blockscout.ts",
    "./observe": "./src/observe.ts"
  },
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@assay/config": "workspace:*",
    "@assay/core": "workspace:*",
    "viem": "^2.52.2"
  }
}
