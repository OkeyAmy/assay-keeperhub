{
  "name": "@assay/core",
  "version": "0.1.0",
  "type": "module",
  "description": "Vendor-neutral intent, verdict and reconciliation primitives. Must not depend on any execution provider.",
  "license": "Apache-2.0",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./intent": "./src/intent.ts",
    "./verdict": "./src/verdict.ts",
    "./reconcile": "./src/reconcile.ts",
    "./receipt": "./src/receipt.ts",
    "./idempotency": "./src/idempotency.ts"
  },
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "viem": "^2.52.2"
  }
}
